/*
 * Comment pairs /* */ cannot nest.
 * "cannot nest" is considered source code,
 * as is the rest of the program.
 */
 int main()
 {
   return 0;
 }
 // We get the following error: error: ‘cannot’ does not name a type
